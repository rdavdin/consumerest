#### An Example of How to use Jackson to consume REST?

- Create a POJO class with fields corresponding to key names in JSON document returned from calling API
- **Bind** the JSON data to the POJO class using **Jackson**